#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

void main(){
    //Definido variaveis
    char letra = 'x';
    //Se a for verdadeiro
    if(letra == 'x'){
        printf("\n A letra eh x ");
    }

    printf("\n O codigo da letra eh %d ", letra);

    if(letra == 120){
        printf("\n A letra eh x ");
    }
    //pausa o programa apos a execução
    system("pause");
}